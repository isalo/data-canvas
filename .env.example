# PostgreSQL connection string used by the demo app and the Drizzle adapter.
# Matches the local database started with `docker compose up -d`.
# If unset, the demo falls back to a seeded in-memory adapter (no persistence).
DATABASE_URL=postgres://datacanvas:datacanvas@localhost:5433/datacanvas
