# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).

When you make a change to a published package, run `pnpm changeset` and describe
the change. The release workflow turns accumulated changesets into version bumps
and changelog entries.
